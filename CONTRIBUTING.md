# المساهمة في أثر

## إعداد بيئة التطوير

1. انسخ `.env.example` إلى `.env` واضبط القيم المحلية.
2. شغّل قاعدة البيانات: `docker compose up -d db`.
3. ثبّت الحزم: `pnpm install`.
4. طبّق الترحيلات: `pnpm db:push`.
5. شغّل المنصة: `pnpm dev`.

## دورة العمل

أنشئ فرعًا قصيرًا لكل تغيير، مثل `feature/project-filters` أو
`fix/mcp-oauth`. لا تُرفع الأسرار أو ملفات `.env` أو مفاتيح توقيع تطبيقات
الجوال. افتح Pull Request إلى `main` واشرح أثر التغيير وطريقة التحقق منه.

قبل كل رفع شغّل:

```bash
pnpm verify
pnpm format:check
```

إذا غيّرت مخطط قاعدة البيانات فأرفق ملفات الترحيل الجديدة داخل `drizzle/`.
وإذا غيّرت إعدادات Capacitor أو أضفت إضافة أصلية فشغّل `pnpm mobile:sync`
وتأكد من تضمين تغييرات المصدر اللازمة في `android/` و`ios/` فقط.

## النشر

بيئة الإنتاج تُدار عبر `docker-compose.prod.yml`. لا تنسخ ملف بيئة الإنتاج
إلى Git. بعد دمج التغيير في `main`، ابنِ الحاويات على الخادم ثم راقب صحة
الخدمات والسجلات قبل اعتبار النشر مكتملًا.
